# HousePrice

Django web app dự đoán giá đất Đà Nẵng bằng Linear Regression.

## Chạy project

```powershell
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Mở trình duyệt tại `http://127.0.0.1:8000/`.
